# Generated by /carthage-annex. Safe to edit; re-running the skill will
# offer to diff before overwriting.
[carthage]
version = "1"                               # config schema version
base_image_tag = "{{ base_image_tag | default('v1') }}"            # carthage-base major this project targets
annexed_with_cli = "{{ annexed_with_cli | default('1.0.3') }}"     # informational; what CLI did the annex
service_name = "{{ service_name | default('dev') }}"
project_slug = "{{ project_slug }}"
