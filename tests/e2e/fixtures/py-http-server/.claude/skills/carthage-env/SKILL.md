---
name: carthage-env
description: Describes the Carthage sandbox environment this project runs inside. Trigger when `$CARTHAGE` is set or user asks orientation questions about the sandbox.
---

If `$CARTHAGE` is set, read `/etc/carthage/SANDBOX.md`. Otherwise this skill is a no-op.
