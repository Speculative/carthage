[carthage]
version = "1"
base_image_tag = "v1"
base_image = "carthage-base:e2e"   # override: tests use a locally-built image, not GHCR
annexed_with_cli = "1.0.0"
service_name = "dev"
project_slug = "e2e-py-http-server"
